---
on:
  push: ~
  pull_request: ~

jobs:
  tests:
    name: Testing with ${{ matrix.python-version }} on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    continue-on-error: ${{ matrix.eperimental == true }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        python-version: ['3.7', '3.8', '3.9', '3.10', '3.11', '3.x']
        include:
        - os: windows-latest
          python-version: '3.11'
          experimental: true
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}
        cache: 'pipenv'
    - name: Setup
      run: |
        pip install pipenv
        pipenv install --dev
    - name: Run tests
      run: pipenv run make test          

